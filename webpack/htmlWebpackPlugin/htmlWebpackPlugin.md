# install
- html-webpack-plugin

# config
```
plugins: [
    new htmlWebpackPlugin({
        filename, // 默认文件名html-webpack-plugin
        inject: '', // head body
        template // 根目录下的index模板
    })
]
```
